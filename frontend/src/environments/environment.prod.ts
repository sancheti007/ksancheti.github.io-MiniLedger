export const environment = {
  production: true,
  // Update this to your deployed backend URL (Railway, Render, Azure, etc.)
  // For GitHub Pages demo mode, the app uses mock data when API is unavailable
  apiUrl: 'https://your-backend-api.railway.app/api'
};
